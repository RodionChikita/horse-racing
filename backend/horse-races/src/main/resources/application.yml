#context-path: /app
#server:
#  port: 8189
#  servlet:
#    context-path: /app
#springdoc:
#
#  api-docs:h: /docs
#
#    pat
#  swagger-ui:
#    filter: true
#    tryItOutEnabled: true
#    path: /swagger
server:
  port: 80

spring:
  flyway:
    enabled: true
    locations: "classpath:db/migration"

  datasource:
    url: jdbc:postgresql://postgres:5432/races
    username: mpu
    password: mpu

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    ROOT: ERROR